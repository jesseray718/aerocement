OPENROOT CONTEXT BRIDGE — 2026-07-11 — Claude (Sonnet) session
Role: paste as message 1 in new AI session. Parse intent, not syntax (voice-to-text user).

## OPERATOR
Jesse (jesseray718) — sole maintainer, OpenRoot appropriate-tech ecosystem.
Termux / Galaxy A15 / no root. Wants: answer-first, radical honesty, status labels
(concept/in-progress/tested/validated), stdlib-only, complete heredoc blocks.

## THIS SESSION — NEW STATE

1. MORAL DECISION FRAMEWORK [concept, research-backed]
   "Love, Limits, and Throughput" — axiom→postulate→speculation synthesis of:
   (a) Mollison/Holmgren permaculture ethics (Mollison 1988 3rd ethic = "setting
       limits," not "fair share" — that's Holmgren 2002),
   (b) Yeshua's one commandment (John 13:34 in-group vs Matt 22:37-40 universal
       — UNRESOLVED CHOICE, One Human Family implies universal, needs written commit),
   (c) Jesse's throughput rule "greatest good per token per unit human input."
   RESULT: throughput rule DEMOTED from objective function to tie-breaker.
   5-GATE LEXICAL PROCEDURE (order is the point):
     G1 Constraints: no person fungible, never sacrifice the one for the sum,
        earth-care limits, maintainer-sustainability floor ("as yourself").
     G2 Need-before-surplus: worst-off reachable user first (maximin, lost-sheep).
     G3 Throughput as tie-breaker ONLY among G1/G2 survivors:
        good/input=(benefit×urgency×(1−slump))/(cost×effort×verification_overhead).
     G4 Surplus returns to earth/people care, not accumulation.
     G5 Publish reasoning — unfalsifiable love-claims count as failures.
   PRECEDENT: Fletcher's Situation Ethics (1966) fused agape+greatest-good and
   collapsed into act-utilitarianism; Taurek 1977 = non-aggregation; "as yourself"
   = symmetry axiom, NOT aggregation license.
   FAILURE TEST: if G1/G2 never bind and the formula always wins, we've rebuilt
   act-utilitarianism and must admit it.
   REC: rename rule → "meet the most real needs per unit maintainer-input
   without sacrificing anyone."

2. SYSTEM-MONITOR BUGS [near-certain from output; source unseen; FIX PENDING]
   - Battery "3.28°C" = 10× decimal error. Android reports tenths of °C
     (328 = 32.8°C); script likely /100 not /10. Same error class as H-003.
   - CPU flat 0.0% across 7 samples = dead metric. Likely single /proc/stat
     read with no prev-sample delta, or silent parse fail defaulting 0.
   - Log lines concatenate (missing \n).
   NEXT: paste monitor source for line-level fixes, or request hardened spec.

## STANDING QUEUE (unchanged)
1. H-003 pinned issue — publicly name ~10× radiative-capture decimal error
   (12.91 claimed vs 0.5–1.5 kWh/m²/night real ceiling). BEACON-01 artifacts
   ready. One evening. Highest leverage.
2. GOVERNOR-01 budget ledger ~/.aiq/budget.json — load-bearing, unbuilt.
3. GROK-ONBOARD-01.md — staged, NOT critically reviewed (bus-factor-1 risk).
4. Claude for Open Source — follow up on application.
5. Protocol B radiative-cooling test, 1–2 m², Sikeston climate — #1 physical val.

## GOVERNANCE/ROUTING STATE
Decision rule: 5-gate procedure [concept — supersedes raw good/input as
top-level; formula survives at G3 only].
Firewalls: swarm never touches publish/push/mint. Backpressure: 10 parked.
Routing: Gemini 2.5 Flash deep reasoning; Groq fast iteration (tokens/day
binds first); Cerebras deprecated; never hardcode model names.

## ANCHORS
github.com/jesseray718 | jrm8908@proton.me | DOI 10.5281/zenodo.20639511
CC-BY-SA-4.0 docs/hardware, GPL-3.0 code.
